/*******************************************
  Shared between samples (except for the simple sample code)
*******************************************/

module.exports.createData = function createData() {
  return new Array('one', 'two', 'three', 'four');
}

module.exports.BORDER_REPEAT = 20;
